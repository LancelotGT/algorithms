# Number of comparisons for quick sort
This code uses three partition method `first element`, `last element` and `median-of-three`. It takes an input file, sort it and output the total number of comparisons when using each partition method.
